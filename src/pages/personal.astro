---
import { pageMeta } from "../data/pageMeta";
import SiteLayout from "../layouts/SiteLayout.astro";

const meta = pageMeta.personal;
---
<SiteLayout title={meta.title} page="personal" pageTag={meta.pageTag} />
