---
import { pageMeta } from "../data/pageMeta";
import SiteLayout from "../layouts/SiteLayout.astro";

const meta = pageMeta.music;
---
<SiteLayout
  title={meta.title}
  description={meta.description}
  page="music"
  pageTag={meta.pageTag}
/>
