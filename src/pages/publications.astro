---
import { pageMeta } from "../data/pageMeta";
import SiteLayout from "../layouts/SiteLayout.astro";

const meta = pageMeta.publications;
---
<SiteLayout title={meta.title} page="publications" pageTag={meta.pageTag} />
